<?xml version="1.0" encoding="UTF-8"?>
<faces-config version="4.0"
              xmlns="https://jakarta.ee/xml/ns/jakartaee"
              xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
              xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee 
              https://jakarta.ee/xml/ns/jakartaee/web-facesconfig_4_0.xsd">

    <!-- No necesitas navigation rules con h:button y h:link -->
    <!-- JSF 4.0 maneja la navegación automáticamente basada en los nombres de archivo -->
    
    <!-- Configuración de navegación opcional si necesitas reglas específicas -->
    <navigation-rule>
        <from-view-id>/login.xhtml</from-view-id>
        <navigation-case>
            <from-outcome>success</from-outcome>
            <to-view-id>/usuarios.xhtml</to-view-id>
            <redirect />
        </navigation-case>
    </navigation-rule>
    
    <navigation-rule>
        <from-view-id>/registro.xhtml</from-view-id>
        <navigation-case>
            <from-outcome>success</from-outcome>
            <to-view-id>/login.xhtml</to-view-id>
            <redirect />
        </navigation-case>
    </navigation-rule>

    <!-- Configuración de beans managed si no usas CDI -->
    <!--
    <managed-bean>
        <managed-bean-name>usuarioBean</managed-bean-name>
        <managed-bean-class>mx.tecnm.toluca.usuarios.control.UsuarioBean</managed-bean-class>
        <managed-bean-scope>view</managed-bean-scope>
    </managed-bean>
    -->

</faces-config>